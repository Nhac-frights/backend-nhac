spring.application.name=backend_nhac




spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/nhac_db?serverTimezone=UTC&rewriteBatchedStatements=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:senai2026}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# ==============================================================================
# Pool de conexões (HikariCP) — dimensionado para Aiven for MySQL
# ==============================================================================
# A Aiven calcula o max_connections DO SERVIÇO pelo RAM do plano (não é
# configurável, só sobe mudando de plano):
#     RAM <  4 GiB -> 75 x RAM + 1
#     RAM >= 4 GiB -> 100 x RAM + 1
#     1 GiB = 76 | 2 GiB = 151 | 4 GiB = 401 | 8 GiB = 801 | 16 GiB = 1601
#     32 GiB = 3201 | 64 GiB = 6401 | 128 GiB = 12801 | 192 GiB = 19201
# (planos pagos da Aiven para MySQL começam em 4 GiB; Free/Developer/Hobbyist
#  são 1 GiB = 76 conexões.)
#
# ATENÇÃO: além disso existe um limite POR USUÁRIO (max_user_connections) que a
# Aiven não expõe nem documenta — o backend usa um único usuário, então é ELE
# que vale, não o max_connections do serviço. A própria Aiven recomenda tratar o
# max_connections como "otimista" (~18 MB por conexão) e começar por uma fração
# dele, aumentando só depois de medir. Por isso os valores abaixo são modestos e
# ajustáveis por env var: DB_POOL_MAX / DB_POOL_MIN.
#
# Regra prática ao mexer:
#   - 1 instância do backend: o total aberto é o DB_POOL_MAX. Mantenha
#     instancias x DB_POOL_MAX bem abaixo do limite por usuário do seu plano.
#   - minimum-idle igual ao maximum-pool-size é a recomendação do HikariCP
#     (pool sempre quente, sem custo de abrir conexão na hora do pico). Se o
#     serviço tiver várias instâncias, prefira reduzir o MAX a deixar conexões
#     ociosas em cada uma.
spring.datasource.hikari.pool-name=NhacHikariPool
spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX:20}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN:20}
# Falha rápido se o pool esgotar (default do Hikari é 30s).
# Conexão ociosa é reciclada depois de 5 min (com min=idle isso vale só para o
# excedente quando minimum-idle for menor que maximum-pool-size).
# Tempo de vida máximo de uma conexão. Precisa ser MENOR que o wait_timeout do
# MySQL (na Aiven o default é 28800s = 8h): 30 min gira a conexão antes de o
# servidor derrubá-la, e sobrevive a failover/manutenção da Aiven.
spring.datasource.hikari.max-lifetime=1800000
# Ping a cada 2 min nas conexões ociosas: detecta conexão morta antes de a
# requisição do usuário esbarrar nela.
spring.datasource.hikari.keepalive-time=120000
# Descomente para investigar conexão presa (loga stacktrace quando alguém
# segura uma conexão por mais de 60s):
#spring.datasource.hikari.leak-detection-threshold=60000
#
# TLS: a Aiven exige SSL nas conexões MySQL. O DB_URL precisa pedir isso, ex.:
#   jdbc:mariadb://<host>:<port>/<db>?sslMode=verify-full&serverSslCert=/etc/ssl/aiven-ca.pem
#   (o ca.pem da Aiven fica em Service > Overview > CA certificate)
# Para teste rápido sem validar o certificado: sslMode=trust
# ==============================================================================
api.security.token.secret=${JWT_SENHA:minha_chave_secreta_local_muito_segura_12345}
spring.jpa.show-sql=false
spring.flyway.out-of-order=true
google.client.id=${CLIENT_GOOGLE_ID:google_cloud_client_id}
twilio.account-sid=${TWILIO_SID:seu_twilio_sid}
twilio.auth-token=${AUTH_TOKEN:seu_twilio_auth_token}
twilio.from-number=${NUMBER:numero}
nhac.sms.mock-mode=true

# Stripe Configuration
stripe.api.key=${STRIPE_API_KEY:sk_test_12345}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_12345}

# Asaas API Configuration (para PIX)
asaas.api.key=${ASAAS_API_KEY:seu_asaas_api_key_aqui}
asaas.api.url=${ASAAS_API_URL:https://sandbox.asaas.com/api/v3}
asaas.webhook.token=${ASAAS_WEBHOOK_TOKEN:seu_token_inventado}

logging.level.br.com.nhac.backend_nhac.domain.auth.TwilioSmsService=INFO

spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
# Brevo (E-mail Transacional)
brevo.api-key=${BREVO_API_KEY:sua_chave_api}
brevo.api-base-url=${BREVO_BASE_URL:https://api.brevo.com/v3}
brevo.sender-email=${BREVO_SENDER_EMAIL:teu_email@gmail.com}
brevo.sender-name=${BREVO_SENDER_NAME:Nhac Delivery}
brevo.timeout-seconds=15
nhac.email.mock-mode=false
spring.flyway.validate-on-migrate=false

# Firebase Storage (upload de imagens de loja e produtos)
# Em mock-mode (padrão), o upload não sobe nada de verdade: só loga e devolve uma URL de placeholder.
# Para ligar de verdade: configure FIREBASE_STORAGE_BUCKET e FIREBASE_CREDENTIALS_BASE64
# (o JSON da service account em Base64) e defina NHAC_STORAGE_MOCK_MODE=false.
firebase.storage.bucket-name=${FIREBASE_STORAGE_BUCKET:nhac-backend.firebasestorage.app}
firebase.storage.credentials-path=classpath:nhac-backend-firebase-adminsdk-fbsvc-f1f23da265.json
firebase.storage.credentials-base64=${FIREBASE_CREDENTIALS_BASE64:}
nhac.storage.mock-mode=${NHAC_STORAGE_MOCK_MODE:false}
nhac.storage.max-tamanho-bytes=5242880
spring.servlet.multipart.max-file-size=6MB
spring.servlet.multipart.max-request-size=6MB

# Janela máxima para considerar GPS do entregador elegível ao despacho
nhac.entrega.localizacao-max-age-seconds=${ENTREGA_LOCALIZACAO_MAX_AGE_SECONDS:120}

# Isolamento explícito para locks/transições concorrentes previsíveis no MariaDB
spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED

# Caffeine: cache local por instancia, somente para consultas publicas de catalogo.
nhac.cache.enabled=${NHAC_CACHE_ENABLED:true}
nhac.cache.catalog-ttl=${NHAC_CACHE_CATALOG_TTL:30s}
nhac.cache.ratings-ttl=${NHAC_CACHE_RATINGS_TTL:2m}
nhac.cache.list-maximum-size=${NHAC_CACHE_LIST_MAXIMUM_SIZE:200}
nhac.cache.detail-maximum-size=${NHAC_CACHE_DETAIL_MAXIMUM_SIZE:2000}
